#include "main.h"

/**
 * 1-isdigit - checks whether a value is a digit
 *
 * Returns: 1 when c is digit, 0 otherwise
 */

int _isdigit(int c);
{
int _isdigit = 0;
int number = 0;

for (; number <= 9; number++)
{
	_isdigit = 1;
}
return (_isdigit);
}
